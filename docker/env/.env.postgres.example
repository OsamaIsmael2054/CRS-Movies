# Postgres container credentials (copy to .env.postgres and edit).
POSTGRES_USER=movies
POSTGRES_PASSWORD=movies
POSTGRES_DB=movies_crs

# Host port the container's 5432 is published on.
POSTGRES_HOST_PORT=5432
